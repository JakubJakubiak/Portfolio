// Local config so Vite does not walk up to the parent
// portfolio project's Tailwind v3 postcss.config.js.
// Tailwind v4 is handled by @tailwindcss/vite in vite.config.js.
export default {
  plugins: {},
}
